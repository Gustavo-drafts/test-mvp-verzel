generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Vehicles {
  id     String @id @default(uuid())
  name   String
  brand  String
  type   String
  imgUrl String

  @@map("vehicles")
}
